import styled from "styled-components";

export const LocationSelectStyle = styled.select`
    width: 68px;
    height: 19px;
    color: var(--main-blue);
    font-weight: 500;
    border: none;
    outline: none;
    margin-right: 47px;
`