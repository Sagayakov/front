import styled from "styled-components";

export const ButtonEnterRegistrationStyle = styled.button`
  width: 199px;
  height: 44px;
  border: 1px solid var(--main-blue);
  border-radius: 8px;
  background-color: var(--white);
  color: var(--main-blue);
  font-size: 16px;
  font-weight: 700;
  cursor: pointer;
`;