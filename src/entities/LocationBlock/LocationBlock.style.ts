import styled from "styled-components";

export const LocationBlockStyle = styled.div`
    width: 164px;
    height: 48px;
    border: 1px solid var(--main-blue);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    & > svg {
        margin-right: 8px;
    }
`